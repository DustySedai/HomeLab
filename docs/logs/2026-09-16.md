# System Log — 2026-09-16

## Summary

Continued configuring the Debian server and began organizing the project documentation.

## Networking

The server's network configuration was investigated.

Relevant components identified on the system included:

- `/etc/network/interfaces`
- `ifupdown`
- `dhcpcd`
- `wpa_supplicant`

The Wi-Fi interface was identified as `wlp2s0`.

The system was using DHCP successfully.

## Static IP Investigation

A static address was tested using:

- Address: `192.168.100.X/24`
- Gateway: `192.168.100.X`

The interface subsequently showed a `linkdown` state and the server became unreachable over SSH.

The configuration was reverted to DHCP, restoring connectivity.

## Documentation

The homelab documentation structure was developed around separate documents for:

- Hardware
- Operating system
- Networking
- Docker
- Services

The project documentation was intended to describe the current state of the server, while daily logs would later document changes and troubleshooting history.

## Notes

The reason the static Wi-Fi configuration failed was left for further investigation.
