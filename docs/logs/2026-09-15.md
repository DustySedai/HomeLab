# System Log — 2026-09-15

## Summary

Continued the initial server validation and hardware testing.

## Hardware Validation

Confirmed the installed memory and storage configuration:

- RAM: approximately 5.6 GiB available to the operating system.
- HDD: 250 GB Hitachi Travelstar 7K500.

## Storage Testing

An extended SMART self-test was started to further evaluate the condition of the HDD.

The disk was considered usable for the homelab, while its previous ATA error-log entries were kept in mind for future monitoring.

## System Testing

CPU stress testing was performed with `stress-ng` to verify basic system stability under load.

The server was monitored during the test as part of the initial hardware validation.

## Networking

The server was operating over Wi-Fi using interface `wlp2s0`.

The initial network configuration used DHCP and provided a LAN address in the `192.168.100.0/24` network.

## Notes

The system was considered suitable for continuing the homelab build.
