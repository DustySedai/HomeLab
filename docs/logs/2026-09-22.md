# System Log — 2026-09-22

## Summary

Expanded the homelab with Immich and updated the network/firewall documentation.

## Immich

Immich was introduced as a self-hosted service.

Relevant configuration included:

- Immich web interface: TCP port `2283`.
- Persistent data under `/srv/immich`.
- Docker networking and port forwarding were reviewed.

## Tailscale

Tailscale was the logic solution to get connectivity from a remote network to the server services.

Implementing this software was pretty easy and simple.

Now we have acces to Immich from any device connected to the Tailscale Network.

## Firewall

The firewall configuration was updated to account for the new service.

The documentation explains why the required service ports are exposed only to the LAN.

## Documentation

The firewall and service configuration were documented and pushed to the GitHub repository.

## Notes

The homelab was beginning to contain multiple independent self-hosted services, making consistent documentation increasingly important.
